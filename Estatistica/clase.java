package Estatistica;

public class clase {
    public static double Clase (int n) {
        double k;
        k = Math.sqrt(n);
        System.out.printf("Quantidade de classes: %.2f", k);
        return k;
    }
}
